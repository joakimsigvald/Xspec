* Given(1), still both default and using
* Given().Default(1), still only default
* Given().Using(1), deprecated
* Using(1), instead of Given().Using(1)
* auto-convert look for first public static method with one argument of type TSource that returns TTarget
* Sequence uniqueness check (From().StartingAt/Spaced): provide a hook to replace the default
  collect-all-values check, for users generating large numbers of values
* Line-ending robustness: Specification text uses Environment.NewLine while expected values in
  Specification.Is follow the source file's line endings, so users with LF files on Windows (or
  CRLF on Linux) get spurious failures. Normalize line endings on both sides of the specification
  comparison (dedicated comparison, not the general string assert). Then renormalize this repo's
  worktree to LF (git rm -r --cached . + git reset --hard) so it matches .gitattributes eol=lf
* Subject identification by declaration instead of parsing: Then<TSubject>(subject) should capture
  subjectExpr via CallerArgumentExpression and register it on the SpecificationContext (like because
  and the verify expressions already do). Then ActualDescriber's Then/And branch stops interpreting
  arguments (drop AsSubject heuristic): cut the chain at the wrapper, describe the tail, and let the
  builder prepend the runtime-registered subject. Also covers the latent case of asserting in the
  same statement as a mock-verify Then<IService>(lambda), where the lambda must not become subject.
  Needs care around the ThreadStatic context and SpecificationBuilder caching, since prefix
  composition moves from parse time (Constraint.Create) to spec-assembly time.
--- before Xspec
* Only show first five items with ellipsis if not all from collection
* Provide custom data generator for Enums and bools to get random values
* CollectionFixture om man vill dela mellan fler klasser
* Separate ClassFixture class from Spec and only allow Arrange on fixture, not when/Then
* Productivity: Auto-generate test classes from Visual Studio
  - rightclick method and generate test class "When..." in the correct folder structure (configurable)
* Documentation: Produce "Javadoc" from code comments
* Documentation: handle nested new(...) in argument list: 
  "=> When(_ => _.AddNewItem(A<CartId>(), new(A<Sku>(), A<ProductType>(), A<Price>(), A<Vat>()), A<string>()));"
* Cast (Result.As<MyType>().MyProperty.Is(123))
  - seems impossible in current C# version
